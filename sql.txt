CREATE DATABASE loginWebsite;
USE loginWebsite;
CREATE TABLE users(
  user_id INT PRIMARY KEY NOT NULL AUTO_INCREMENT,
  username VARCHAR(20) NOT NULL,
  password VARCHAR(60) NOT NULL);

CREATE USER 'userLogin'@'localhost' IDENTIFIED VIA mysql_native_password USING '***';GRANT SELECT, INSERT ON *.* TO 'userLogin'@'localhost' REQUIRE NONE WITH MAX_QUERIES_PER_HOUR 0 MAX_CONNECTIONS_PER_HOUR 0 MAX_UPDATES_PER_HOUR 0 MAX_USER_CONNECTIONS 0; 
